export type ErrorMessage = {
  message: string;
};

export type GlossaryItem = {
  term: string;
  definition: string;
};

export type Guitardex = {
  name: string;
  techniques: number[];
  audioSkills: number[];
};
